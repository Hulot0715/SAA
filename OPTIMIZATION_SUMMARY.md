# 模拟退火算法实验日志

## 实验记录

### 2026-03-19 v1.2
**参数配置外置，提升可维护性**
- 将所有初始参数（`SEED`、`N_CITIES`、`COORD_RANGE`、`ID_LAST2`、`T0`、`T_FINAL`、`ALPHAS`、`INNER_ITER`）从主脚本中提取，集中写入独立配置文件 `config.py`
- 主脚本 `tsp_simulated_annealing.py` 中的参数块替换为 `from config import ...`，逻辑代码零修改
- 调整实验参数只需编辑 `config.py`，无需接触算法主体，降低误改风险
- 新增 `config.py` 至生成文件清单

### 2026-03-19 v1.1
**代码参数配置优化，提升可维护性**
- 移除硬编码常量 `STUDENT_ID = 125130024341`，简化参数区命名为"初始参数配置"
- `ID_LAST2` 改为动态计算：`ID_LAST2 = SEED % 100`，避免与 SEED 不一致的风险
- `calc_distance_matrix` 函数增加行内注释，说明 `np.newaxis` 的维度扩展作用
- `generate_cities` 返回值增加行内注释
- 主程序启动打印精简，去除学号行，输出更简洁

### 2026-03-19 v1.0
**完成 TSP 模拟退火算法完整实现与参数对比实验**
- 以学号 125130024341 后5位（24341）为随机种子，生成个性化50座城市坐标
- 初始温度 T₀ = 1000 + 41×20 = 1820，终止温度 T_final = 1.0
- 实现 2-opt 逆转邻域构造（另备 swap 法、insert 法）
- 严格遵守 Metropolis 接受准则：ΔE<0 无条件接受，ΔE>0 以 e^(−ΔE/T) 概率接受
- 完成 α = 0.85 / 0.92 / 0.99 三组参数对比实验
- 生成路线图、收敛曲线、参数对比汇总图共4张

---

## 实验结果汇总

### 个性化数据参数

| 参数 | 值 |
|:-----|:---|
| 学号 | 125130024341 |
| 随机种子（后5位） | 24341 |
| 城市数量 N | 50 |
| 坐标范围 | 1000 × 1000 |
| 初始温度 T₀ | 1820 |
| 终止温度 T_final | 1.0 |
| 每温度内循环次数 | 200 |

### 参数对比结果

| 退火系数 α | 外循环迭代次数 | 最优路径长度 | 耗时（秒） | 效果评价 |
|:---------:|:------------:|:----------:|:--------:|:-------:|
| 0.85 | 47 | 5751.05 | 0.19 | ⭐ 快速收敛，质量较差 |
| 0.92 | 91 | 5900.50 | 0.40 | ⭐⭐⭐ 中等 |
| 0.99 | 747 | 5587.99 | 3.22 | ⭐⭐⭐⭐⭐ 慢速收敛，质量最优 |

### 结论分析
- **α = 0.99** 找到最短路径 **5587.99**，迭代次数最多（747次），耗时最长（3.22秒）
- **α = 0.85** 仅迭代 47 次，速度最快但路径质量最差（5751.05）
- **α = 0.92** 表现居中，路径长度 5900.50，略逊于 α=0.85（本次随机种子下的特殊情况）
- 总体规律：**退火越慢（α 越大）→ 搜索越充分 → 路径越短（更接近全局最优）**

---

## 生成文件清单

| 文件名 | 描述 |
|:-------|:----|
| `config.py` | 实验参数配置文件（外置参数，统一管理） |
| `tsp_simulated_annealing.py` | 完整可运行算法代码（含注释，参数由 config.py 导入） |
| `tsp_result_alpha085.png` | α=0.85 路线图 + 收敛曲线 |
| `tsp_result_alpha092.png` | α=0.92 路线图 + 收敛曲线 |
| `tsp_result_alpha099.png` | α=0.99 路线图 + 收敛曲线 |
| `tsp_comparison.png` | 三组参数收敛曲线对比汇总图 |
| `OPTIMIZATION_SUMMARY.md` | 本实验日志 |

---

## 算法实现要点

### 邻域构造（2-opt 逆转法）
```python
def neighbor_2opt(tour):
    i, j = sorted(np.random.choice(len(tour), 2, replace=False))
    new_tour[i:j+1] = new_tour[i:j+1][::-1]  # 逆转路径段
    return new_tour
```

### Metropolis 接受准则
```python
if delta_E < 0:
    # 优于当前解 -> 无条件接受
    current_tour, current_len = new_tour, new_len
else:
    # 劣于当前解 -> 以 Boltzmann 概率接受
    if np.random.random() < np.exp(-delta_E / T):
        current_tour, current_len = new_tour, new_len
```

---

## 待完善项

- [ ] 尝试混合邻域（2-opt + insert 自适应切换）
- [ ] 增加自适应内循环次数（随温度调整 inner_iter）
- [ ] 对比贪心初始解与随机初始解的效果差异
- [ ] 实验报告 PDF 撰写与提交（截止：3月30日 23:59:59）
